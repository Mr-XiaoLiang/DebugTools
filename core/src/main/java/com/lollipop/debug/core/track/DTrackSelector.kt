package com.lollipop.debug.core.track

import android.content.Context
import com.lollipop.debug.core.DebugDataProvider

class DTrackSelector(context: Context) : DebugDataProvider<DTrackInfo> {
    override fun select(pageSize: Int, pageIndex: Int): List<DTrackInfo> {
        TODO("Not yet implemented")
    }
}